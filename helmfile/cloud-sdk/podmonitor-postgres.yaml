# this needs to be moved to a separate fake chart
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: postgres-exporter
  namespace: monitoring
  labels:
    release: prometheus-operator
spec:
  selector:
    matchLabels:
      app: postgresql
      release: tip
  podMetricsEndpoints:
  - port: http-metrics
  namespaceSelector:
    any: true
